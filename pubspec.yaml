name: netguard
description: "A powerful HTTP client for Flutter built on top of Dio with enhanced security and networking capabilities."
version: 0.0.1
homepage: https://github.com/nitesh695/NetGuard
repository: https://github.com/nitesh695/NetGuard
issue_tracker: https://github.com/nitesh695/NetGuard/issues

environment:
  sdk: ^3.8.1
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.4.0
  meta: ^1.9.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  mockito: ^5.4.4
  build_runner: ^2.4.7

flutter:

